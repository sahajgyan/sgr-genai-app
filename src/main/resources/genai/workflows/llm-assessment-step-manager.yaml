id: "assessment-analyzer-workflow"
name: "Standard Math Assessment"
version: "1.0"
type: "CHAIN"  # <--- Indicates a linear sequence
steps:
  - stepId: "assessment-analyzer-agent"
    agentId: "assessment-analyzer-agent" # Uses the first prompts I gave you
    inputSource: "USER_INPUT" # The raw list of questions