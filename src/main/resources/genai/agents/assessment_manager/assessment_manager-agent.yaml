id: "assessment_manager"
name: "Assessment Workflow Orchestrator"
model:
  provider: "gemini" # or openai
  name: "gemini-flash-latest" # Needs to be fast and cheap, it reads a lot of data
  temperature: 0.0
systemPromptPath: "prompts/system.md"
metadata:
  outputMode: "json"